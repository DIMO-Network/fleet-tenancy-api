apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fleet-tenancy-api.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "fleet-tenancy-api.labels" . | nindent 4 }}
data:
{{- range $key, $val := .Values.env }}
  {{ $key }} : {{ $val | quote}}
{{- end}}